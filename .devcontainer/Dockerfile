# Context: SysML and Ontology Repository
# Reason: Provides lightweight isolation for markdown and system model linting.
FROM mcr.microsoft.com/devcontainers/alpine:3.18

RUN apk update && apk add --no-cache git
